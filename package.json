{
  "name": "snislab-home-assistant-apps",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-apps.mjs",
    "validate": "node scripts/validate-apps.mjs",
    "test": "npm run validate && node --test"
  },
  "dependencies": {
    "yaml": "^2.9.1"
  },
  "engines": {
    "node": ">=20"
  }
}
